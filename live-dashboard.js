document.addEventListener('DOMContentLoaded',()=>{const items=[...document.querySelectorAll('[data-live-value]')];if(!items.length)return;const parse=(el)=>({min:Number(el.dataset.min||0),max:Number(el.dataset.max||100),suffix:el.dataset.suffix||'',prefix:el.dataset.prefix||''});const tick=()=>{items.forEach(el=>{const c=parse(el);const value=Math.floor(c.min+Math.random()*(c.max-c.min+1));el.textContent=`${c.prefix}${value}${c.suffix}`;el.classList.add('live-pulse');setTimeout(()=>el.classList.remove('live-pulse'),360)});};tick();setInterval(tick,2600);});